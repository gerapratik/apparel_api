// src/models/Apparel.ts

export interface ApparelItem {
    code: string;
    size: string;
    quantity: number;
    price: number;
  }
  
  export interface OrderItem {
    code: string;
    size: string;
    quantity: number;
  }
  